# CSV Import v3.6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a working CSV import entry and deliver version 3.6 validated with the user's 128-row export.

**Architecture:** A pure Kotlin parser converts exported CSV into validated import rows. The repository filters duplicates and inserts new entities, while the existing data-transfer screen uses Android's system file picker to preview and confirm the import.

**Tech Stack:** Kotlin, Jetpack Compose, Room, Android Activity Result APIs, JUnit.

---

### Task 1: CSV parser

**Files:**
- Create: `app/src/main/java/com/expense/tracker/data/importer/CsvExpenseImporter.kt`
- Create: `app/src/test/java/com/expense/tracker/data/importer/CsvExpenseImporterTest.kt`

- [ ] Write parser tests for BOM, quoted commas, escaped quotes, embedded newlines, invalid rows and the required header.
- [ ] Run the tests and confirm they fail because the parser does not exist.
- [ ] Implement the minimal RFC 4180-compatible parser and row validation.
- [ ] Run the parser tests and confirm they pass.

### Task 2: Duplicate-safe repository import

**Files:**
- Modify: `app/src/main/java/com/expense/tracker/data/db/ExpenseDao.kt`
- Modify: `app/src/main/java/com/expense/tracker/data/repo/ExpenseRepository.kt`
- Modify: `app/src/test/java/com/expense/tracker/data/repo/RepositoryTest.kt`

- [ ] Add failing tests proving existing and within-file duplicates are skipped.
- [ ] Add a query for all rows and a batch insert DAO operation.
- [ ] Implement repository import while preserving source timestamps and generating new IDs.
- [ ] Run repository tests and confirm they pass.

### Task 3: Import entry and confirmation

**Files:**
- Modify: `app/src/main/java/com/expense/tracker/ui/settings/DataExportScreen.kt`
- Modify: `app/src/main/java/com/expense/tracker/ui/settings/SettingsMenuScreen.kt`
- Modify: `app/src/main/java/com/expense/tracker/MainActivity.kt` only if route labels require it.

- [ ] Add an `OpenDocument` CSV picker.
- [ ] Parse the selected file off the main thread.
- [ ] Show valid count, date range and invalid count in a confirmation dialog.
- [ ] Import valid rows and report inserted, duplicate and invalid counts.
- [ ] Rename visible labels to “数据导入与导出”.

### Task 4: v3.6 and real-file verification

**Files:**
- Modify: `app/build.gradle.kts`
- Modify: `README.md`
- Produce: `C:/Users/fuker/Desktop/app/codex change/ai-expense-bot-v3.6-csv-import-2026-07-30.apk`

- [ ] Set versionName to `3.6` and versionCode to `27`.
- [ ] Parse the supplied CSV and confirm 128 valid rows with zero invalid rows.
- [ ] Run all unit tests and build the Debug APK.
- [ ] Verify package metadata, signature and SHA-256 before delivery.
