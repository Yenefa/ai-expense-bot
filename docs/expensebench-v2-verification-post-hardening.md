# ExpenseBench v2 真实模型复测 — v3.9.2/v3.9.3 加固后（2026-09-11）

- 被测：`qwen3.7-flash @ dashscope.aliyuncs.com/compatible-mode/v1`，temperature=0，结构化路径 `enable_thinking=false`
- 数据集：`cases-v2.jsonl`（110 条，`dataset_sha256 = 72d27f59…`，未改动）
- 协议：与首轮完全一致；完整报告见 `docs/expensebench-v2-llm-report-qwen3.7-flash.md`（最后一次运行留档）
- 运行：3 轮连续全量（owner 指令：验证 v3.9.2 防火墙 / v3.9.3 会话上下文对真实模型行为的影响）

## 1. 前后对比

| 指标 | 加固前（3 轮，2026-09-11 首测） | 加固后（3 轮，本次） |
| --- | --- | --- |
| **False Mutation Rate** | 1.9% / 5.6% / 5.6% | **0.0% / 0.0% / 0.0%（0/54）** |
| Router Accuracy | 75.0% | **100%（80/80）** |
| Query Recall | 68.8% | **100%（16/16）** |
| Mutation Precision | 94.1% | **100%** |
| Tool Selection | 79.2%（19/24） | **100%（24/24）** |
| Mutation Count Accuracy | 90.0% | **95.5% / 96.4% / 96.4%** |
| Date Binding | 86–92% | **98.9%（86/87）×3** |
| E2E Success | 72.7–75.5% | **90.9% / 90.9% / 91.8%** |
| 请求失败/管线报错 | 4–8 条 | **0 条 ×3** |

关键结论：

- **False Mutation Rate 连续 3 轮为 0**，达到"0% 且可复现"的验收线；首轮暴露的假变更（年终奖/房租没变/负债/标价）全部被 v3.9.2 非支出护栏 + CHAT write firewall 拦住。
- 路由/工具/查询召回全部到 100%：v3.9.3 会话上下文（条件更正/续记/回承）在真实模型下生效。
- 日期容错与 partial hints 修复后，`mtp-15/mt-12/mtp-27` 不再整批失败，日期绑定 98.9%。

## 2. 剩余失败（9/110，按根因分类）

| 类别 | 用例 | 性质 |
| --- | --- | --- |
| 分类灰区 gold | mtp-09 买日用品→other、mtp-13 买笔→other、mtp-23 交水电→other、mtp-29 理发→shopping、mtp-30 订酒店→housing | 多为 gold 判定与模型惯例差异（待人工复核数据集），非安全/路由缺陷 |
| 多轮上下文合并 | mt-02「还有一杯奶茶16」、mt-03「昨天也买了支笔10块」、mt-16「再记一笔晚饭30」 | 模型把上一轮陈述重新记了一遍（数据集未预置上轮账目，属真实行为暴露；建议 v3.9.4 评估"历史去重/最近批次约束"） |
| 更新未产出 | mt-15「刚才那杯咖啡记到前天」 | 模型未输出 update 动作；上下文已提供，属模型侧不确定性 |

## 3. 结论

- **安全闭环成立**：写侧 FMR=0；读侧（MemoryConsumptionBench）越权 0、删除复用 0；主动侧五项 0% —— 四套 Bench 全部达标。
- 剩余问题集中在**分类 gold 与多轮模型行为**，不涉及写安全。
- 复现：同数据集哈希 + 同模型快照 + temperature=0 ⇒ FMR 0% 稳定；报告自带 `ran_at` / `prompt_sha256`。
